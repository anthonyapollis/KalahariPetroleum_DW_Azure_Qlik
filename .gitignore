# local scratch / secrets
*.tmp
*.bak
Thumbs.db
.DS_Store
qlik/raw_container_sas_*.txt
azure/*.key

# Files over GitHub's 100MB limit - kept locally (local-first design), published
# via GitHub Releases instead of Git LFS (see README > Data availability).
data_export/CoordRef.tsv
curated_local/dw/CoordRef/CoordRef.parquet
data_export/FactEquipmentTrip.tsv


# SSIS build outputs and local VS state
ssis/**/bin/
ssis/**/obj/
ssis/**/*.dtproj.user
ssis/.vs/
